import React from 'react';
import PropTypes from 'prop-types';

const Main = (props) => {
    return (
        <div>
            <h3 className='main'>Вход</h3>
            <input type="text"/>
        </div>
    );
};

Main.propTypes = {};
Main.defaultProps = {};

export default Main;
