import moment from "moment/moment";

export const getCosts = state => state.costs;
export const click = state => state.click;
export const period = state => state.period;
export const spent = state => state.spent.toLowerCase();










